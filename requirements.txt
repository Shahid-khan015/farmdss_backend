# Pinned to the versions this app is known to run on. Unpinned ranges meant a hosted build
# installed whatever was newest that day, which is not what was tested locally.
fastapi==0.128.8
starlette==0.49.3
uvicorn[standard]==0.39.0
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12
alembic==1.16.5
pydantic==2.13.5
pydantic-settings==2.11.0
python-dotenv==1.2.1
python-multipart==0.0.20
httpx[http2]==0.28.1
paho-mqtt==2.1.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
python-jose[cryptography]==3.5.0

reportlab==5.0.1

# Testing
pytest==8.4.2
